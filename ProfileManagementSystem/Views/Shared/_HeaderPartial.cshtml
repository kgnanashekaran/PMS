@*<div class="app-header-left">
        <div class="search-wrapper">
            <div class="input-holder">
                <input type="text" class="search-input" placeholder="Type to search">
                <button class="search-icon"><span></span></button>
            </div>
            <button class="close"></button>
        </div>
        <ul class="header-menu nav">
            <li class="nav-item">
                <a href="javascript:void(0);" class="nav-link">
                    <i class="nav-link-icon fa fa-database"> </i>
                    Profile Management System
                </a>
            </li>

        </ul>
    </div>*@
<div class="app-header-right">
    <div class="header-btn-lg pr-0">
        <div class="widget-content p-0">
            <div class="widget-content-wrapper">
                <div class="widget-content-left">
                    <div class="btn-group">
                        <a data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" class="p-0 btn" style="margin-right:10px;">
                            @if (HttpContext.Current.Session["fName"] == null)
                            {
                                <img width="42" class="rounded-circle" src="../assets/images/profiles/ProfilePic.png" alt="">
                            }
                            else
                            {
                                <img width="42" class="rounded-circle" src="../assets/images/profiles/@HttpContext.Current.Session["id"].ToString()" alt="" >
                                <i class="fa fa-angle-down ml-2 opacity-8"></i>                            
                                @*<i class="glyphicon glyphicon-menu-down"></i>*@

                            }

                        </a>
                        @if (HttpContext.Current.Session["fName"] == null)
                        {

                            @*<div tabindex="-1" role="menu" aria-hidden="true" class="dropdown-menu dropdown-menu-right">*@
                            @*<button type="button" tabindex="0" class="dropdown-item">Logged in as @HttpContext.Current.Session["role"].ToString() </button>
                                <button type="button" tabindex="0" class="dropdown-item">Change Password</button>
                                <div tabindex="-1" class="dropdown-divider"></div>*@
                            @*<button type="button" tabindex="0" class="dropdown-item">Login</button>*@
                            @*</div>*@
                        }
                        else
                        {
                        <div tabindex="-1" role="menu" aria-hidden="true" class="dropdown-menu dropdown-menu-right">
                            <button type="button" tabindex="0" class="dropdown-item">Logged in as @HttpContext.Current.Session["role"].ToString() </button>
                            <button type="button" tabindex="0" class="dropdown-item" onclick=onButtonclick(1);>Administration </button>
                            @*<button type="button" tabindex="0" class="dropdown-item">Change Password</button>*@
                            @*<h6 tabindex="-1" class="dropdown-header">Header</h6>*@
                            @*<button type="button" tabindex="0" class="dropdown-item">Actions</button>*@
                            <div tabindex="-1" class="dropdown-divider"></div>
                            <button type="button" tabindex="0" class="dropdown-item" onclick=onButtonclick(2);>Logout</button>
                        </div>
                        }
                    </div>
                </div>
                @*@HttpContext.Current.Session["id"].ToString()*@
                <div class="widget-content-left  ml-3 header-user-info" style="margin-left: 0px !important;">
                    <div class="widget-heading">

                        @if (HttpContext.Current.Session["fName"] != null)
                        {
                          var name=  char.ToUpper(HttpContext.Current.Session["fName"].ToString()[0]).ToString() + HttpContext.Current.Session["fName"].ToString().Substring(1).ToLower();
                            @name
                        }
                        else
                        {
                            <a class="mybtn btn btn-link btn-xs" style="font-weight:bold;" href="@Url.Action("Index", "Login")">
                                Admin Login
                            </a>
                            @*<b> <button type="button" class="btn btn-link" onclick="@Url.Action("Index", "Login")">Login</button></b>*@
                        }
                    </div>
                    @*<div class="widget-subheading">
                            @if (HttpContext.Current.Session["role"] != null)
                            {
                                HttpContext.Current.Session["role"].ToString();
                            }
                        </div>*@
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    function onButtonclick(id) {
        if (id == 1) {
            location.href = '@Url.Action("Index","Profile")';
        }
         if (id == 2) {
            location.href = '@Url.Action("Logout", "Login")';
        }
    }
    </script>